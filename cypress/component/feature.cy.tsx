// cypress/integration/featured-test.cy.tsx
/*
import Featured from "@/app/components/featured/Featured";
import styles from "@/app/components/featured/Featured.module.css";

describe("Featured Component", () => {
  it("renders the Featured component", () => {
    // Mount the component
    cy.mount(<Featured />);

    // Wait for the data fetching to complete

    // Check if the container element exists
    cy.get(`.${styles.container}`).should("exist");

    // Check if the title element exists
    cy.get(`.${styles.title}`).should("exist");

    // Check if the post element exists
    cy.get(`.${styles.post}`).should("exist");

    // Check if the image element exists
    cy.get(`.${styles.imageContainer} img`).should("exist");

    // Check if the text container element exists
    cy.get(`.${styles.textContainer}`).should("exist");

    // Check if the post title element exists
    cy.get(`.${styles.postTitle}`).should("exist");

    // Check if the post description element exists
    cy.get(`.${styles.postDesc}`).should("exist");

    // Check if the "Read More" link exists
    cy.get(`.${styles.link}`).should("exist");
  });
});
*/
